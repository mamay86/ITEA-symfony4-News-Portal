{% extends 'base.html.twig' %}

{% block title %}ITEA News Portal{% endblock %}

{% block main %}
    <main role="main">
        <!--<section class="jumbotron text-center">
            <div class="container">
                <h1 class="jumbotron-heading">{{ page.getFirstBlockTitle }}</h1>
                <p class="lead text-muted">
                    {{ page.getFirstBlockText }}
                </p>
                <p>
                    <a href="#" class="btn btn-primary my-2">
                        {{ page.getFirstBlockMainBtn }}
                    </a>
                    <a href="#" class="btn btn-secondary my-2">
                        {{ page.getFirstBlockActionBtn }}
                    </a>
                </p>
            </div>
        </section>
        <div class="categories">
            <h2>Categories</h2>
            <ul class="list">
                {% for category in categories %}
                    <li><a href="category/{{ category.id }}">{{ category.title }}</a></li>
                {% endfor %}
            </ul>
        </div>
        <div class="album py-5 bg-light">
            <div class="container">
                <div class="row">
                    {% for post in latest_posts %}
                        <div class="col-md-4">
                            <div class="card mb-4 shadow-sm">
                                <img class="card-img-top" src="img/{{ post.image }}" alt="">
                                <div class="card-body">
                                    <b>{{ post.category.title }}</b>
                                    <p class="card-text">
                                        {{ post.title }}
                                    </p>
                                    <div class="d-flex justify-content-between align-items-center">
                                        <div class="btn-group">
                                            <a href="{{ path('post_view', {'id': post.id}) }}" class="btn btn-sm btn-outline-secondary">Read</a>
                                        </div>
                                        <small class="text-muted">9 mins</small>
                                    </div>
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                </div>
            </div>
        </div>-->
    </main>
{% endblock %}